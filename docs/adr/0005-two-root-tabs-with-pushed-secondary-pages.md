# Use two root tabs with pushed secondary pages

The MVP root layout has Home and Category as equal tabs, using HdsTabs when its API 23 fit is validated and ArkUI Tabs otherwise. Topic detail, login, and reply are secondary pages pushed through AppShell navigation, preserving the reference client's core information architecture without treating authentication or composing as root destinations.
