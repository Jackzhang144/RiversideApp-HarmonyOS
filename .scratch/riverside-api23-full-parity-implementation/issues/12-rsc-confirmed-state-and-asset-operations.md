# 12 — RSC 状态与资产操作确认流

**What to build:** 让用户在每一笔明确确认后执行 RSC 转账、打赏、红包创建/关闭和每日奖励领取等操作，并在结果后刷新钱包与账本。

**Blocked by:** 11 — RSC 只读钱包与账本.

**Status:** ready-for-agent

- [ ] 每项状态或资产操作在请求发送前单独取得用户确认；取消、失败、重复点击和超时均不能制造重复写入。
- [ ] 只在用户本次明确同意后做真实服务写入；保留操作前后可核验结果。
- [ ] 完成构建、自动化和逐笔人工确认的真实服务验证记录。
