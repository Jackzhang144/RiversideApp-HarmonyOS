#!/usr/bin/env bash

set -Eeuo pipefail

readonly PROJECT_ROOT="$(git rev-parse --show-toplevel)"

"$PROJECT_ROOT/scripts/check-staged-sensitive-config.sh"
"$PROJECT_ROOT/scripts/check-code-health.sh" --staged
